# Archive folder + Issue-prompt UI (prototype)

**Question:** What should the Invoices archive-folder controls and post-Issue flows look like?

**Plan:** Three variants on `/prototype?variant=A|B|C`, switchable via floating bar. Stubs only — no FS Access. Not mounted on `/invoices`.

| Key | Name | Structure |
|-----|------|-----------|
| A | Quiet status line | Folder + Change/Clear as text under Issued; zip text link; post-Issue = top banners |
| B | Setup panel + modal | Dedicated archive panel; “Allow on every visit” tip; missing-root = overlay |
| C | Issue-adjacent chip | Folder chip beside **mock** Preview/Issue row (not the live Invoices header); sticky bottom outcome strip; Clear in ··· menu |

**Verdict:** _(fill after reaction — which bits to keep)_
